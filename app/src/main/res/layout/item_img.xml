<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <variable
            name="viewModel"
            type="com.example.instagramgallery.GalleryViewModel" />

        <variable
            name="selectedImage"
            type="com.example.instagramgallery.SelectedImage" />

        <import type="android.view.View" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="110dp"
        android:layout_height="110dp">

        <ImageView
            android:id="@+id/iv"
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:layout_width="27dp"
            android:layout_height="27dp"
            android:layout_marginTop="8dp"
            android:layout_marginRight="13dp"
            android:background="@drawable/shape_select_picture_circle_bg"
            android:gravity="center"
            android:textColor="#FFFFFF"
            android:text="@{selectedImage.index.toString()}"
            android:visibility="@{viewModel.isMultiSelect ? View.VISIBLE : View.GONE}"
            app:layout_constraintEnd_toEndOf="@+id/iv"
            app:layout_constraintTop_toTopOf="parent" />


    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>